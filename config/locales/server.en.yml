en:
  site_settings:
    json_s3_export_enabled: "Is Json S3 Exporter enabled?"
    json_s3_export_access_key: 'What is AWS S3 access key?'
    json_s3_export_secret_key: 'What is AWS S3 secret key?'
    json_s3_export_region: 'What AWS region is your bucket in?'
    json_s3_export_bucket: 'What is the S3 to upload the data to?'
    json_s3_export_clear_files_before_upload: 'Clear the existing files before uploading?  This will remove uploaded files.  Make backups if needed.'
